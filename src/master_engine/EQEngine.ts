export function aplicarEQBasica(buffer: Float32Array) {
  // En un entorno real, esto usaría BiquadFilterNode o convolución.
  // Por ahora, devolvemos el buffer intacto como placeholder estructural.
  return buffer;
}
